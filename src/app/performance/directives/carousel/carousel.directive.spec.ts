import { CarouselDirective } from './carousel.directive';

describe('CarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
